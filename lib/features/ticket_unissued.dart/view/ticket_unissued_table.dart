import 'package:flutter/cupertino.dart';

class TicketUnissuedTable extends StatelessWidget {
  const TicketUnissuedTable({ super.key });

  @override
  Widget build(BuildContext context) {
    return Text('未出票table');
  }
}